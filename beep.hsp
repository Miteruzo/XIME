#uselib "kernel32
#func beep "Beep" int,int
#pack "start.ax
#packopt hide 1
#packopt name "beepplay

	sdim cline,,2
	cline=dir_cmdline
	cline=strtrim(cline,3,' ')
	split cline,",",cline
	if cline(1)="": cline(1)="100
	beep 0+440.0*powf(2,double(cline(0))/1200),0+cline(1)
	end
